# Personal state — never commit
cortex-log.jsonl
cortex-doctor-last-run
proposals/
retros/

# OMC working state — session ids, agent replays, model transcripts
.omc/

# Cortex Command — build artifacts + deps (node_modules, dist, tsbuildinfo)
command/node_modules
command/**/dist
command/**/*.tsbuildinfo

# Standard
__pycache__/
*.pyc
.DS_Store
.venv/
*.swp
.idea/
.vscode/
