# Personal state — never commit
cortex-log.jsonl
cortex-doctor-last-run
proposals/
retros/

# OMC working state — session ids, agent replays, model transcripts
.omc/

# Standard
__pycache__/
*.pyc
.DS_Store
.venv/
*.swp
.idea/
.vscode/

# Eval harness outputs (results + unlabelled worksheets) live in the state dir
eval/out/

# Optional adviser records when CORTEX_HOME points at the checkout
experiments/jev/

# Build output left behind by the M1 UI branch (source is tracked on
# feat/cortex-command; only dist/ + node_modules survive a branch switch)
command/
