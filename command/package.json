{
  "name": "cortex-command",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Drive Claude Code from a browser — chat workspace + autonomous run-queue.",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace web",
    "dev": "npm run build --workspace shared && (npm run dev --workspace server & npm run dev --workspace web & wait)",
    "start": "node server/dist/index.js",
    "clean": "npm run clean --workspaces --if-present"
  }
}
